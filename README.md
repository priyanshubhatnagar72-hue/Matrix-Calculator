# Create a README.md file based on the matrix calculator code provided.

readme_content = """# Matrix Operations CLI

A lightweight Command Line Interface (CLI) tool built with Python and NumPy to perform fundamental matrix operations. This script allows users to input dimensions and elements dynamically and execute various linear algebra transformations.

## 🚀 Features

* **Dynamic Input:** Define matrix dimensions and elements directly from the terminal.
* **Arithmetic Operations:** Supports addition, subtraction, and dot product multiplication.
* **Linear Algebra Tools:** * Transpose of a matrix.
    * Determinant calculation.
    * Matrix inversion.

## 🛠️ Prerequisites

Before running the script, ensure you have Python installed and the **NumPy** library available.

```bash
pip install numpy
